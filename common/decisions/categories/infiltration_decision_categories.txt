communist_infiltration = {
	allowed = {
		OR = {
			original_tag = JAP
			original_tag = ECA ##MODDED
			original_tag = MAN
			is_subject_of = JAP
		}
	}

	visible = {
		is_subject = no
		if = {
			limit = { original_tag = JAP }
			any_other_country = {
				is_subject_of = ROOT
			}
		}
		NOT = {
			has_government = communism
		}
		any_other_country = {
			has_completed_focus = PRC_anti_japanese_expedition
		}
	}
}
