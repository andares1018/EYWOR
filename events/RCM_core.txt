###########################
# Chinese Events BORDER WARS ADDED SOUTHERN SHAANXI #ID = 743#
###########################

add_namespace = core

# core on annex

country_event = {
	id = core.1
	title = core.1.t
	desc = core.1.d
	picture = GFX_report_event_core
	is_triggered_only = yes
	option = { 
		name = core.1.a #accept
		hidden_effect = {
			release = event_target:core_target
			end_puppet = event_target:core_target
			every_state = {
				limit = {
					is_owned_by = event_target:core_target
					#is_owned_by = ROOT
					has_state_flag = core_flag
				}
				add_core_of = ROOT
				clr_state_flag = core_flag
			}
		}
		effect_tooltip = {
			every_state = {
				limit = {
					#is_owned_by = event_target:core_target
					is_owned_by = ROOT
					has_state_flag = core_flag
				}
				add_core_of = ROOT
				clr_state_flag = core_flag
			}
		}
		hidden_effect = {
			annex_country = {
				target = event_target:core_target
				transfer_troops = yes
			}
		}
	}
}
